import { StyledNav } from "./Nav.styled";
import NavListContainer from "../../components/NavListContainer/NavListContainer";

const Nav = () => {
  return (
    <StyledNav>
      <NavListContainer />
    </StyledNav>
  );
};

export default Nav;
